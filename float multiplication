#include <stdio.h>

int main() {
    float a, b, answer;

    printf("Enter first number: ");
    scanf("%f", &a);

    printf("Enter second number: ");
    scanf("%f", &b);

    printf("Product: %f\n", a*b);

    return 0;
}
